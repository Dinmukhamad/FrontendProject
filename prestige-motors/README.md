# PRESTIGE MOTORS - Luxury Car Dealership Website

Полнофункциональный веб-сайт автосалона премиум-класса с системой регистрации, авторизации и базой данных.

## 🚀 Технологии

- **Backend**: Python Flask
- **Database**: SQLite (SQLAlchemy ORM)
- **Authentication**: Flask-Login + Bcrypt
- **Frontend**: HTML5, CSS3, JavaScript
- **Design**: Современный темный дизайн с золотыми акцентами

## 📋 Возможности

### Для пользователей:
- ✅ Регистрация и авторизация
- ✅ Просмотр коллекции автомобилей
- ✅ Добавление автомобилей в избранное (❤️)
- ✅ Отправка запросов через контактную форму
- ✅ Личный профиль с историей запросов
- ✅ Управление избранными автомобилями

### Для администраторов:
- ✅ Панель администратора
- ✅ Управление автомобилями (добавить/редактировать/удалить)
- ✅ Просмотр всех запросов клиентов
- ✅ Статистика пользователей и автомобилей

## 🛠️ Установка

### 1. Клонируйте репозиторий или скачайте файлы

### 2. Создайте виртуальное окружение

```bash
python -m venv venv
```

### 3. Активируйте виртуальное окружение

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 4. Установите зависимости

```bash
pip install -r requirements.txt
```

## 📁 Структура проекта

```
prestige-motors/
│
├── app.py                      # Главное приложение Flask
├── requirements.txt            # Зависимости Python
├── prestige_motors.db         # База данных SQLite (создается автоматически)
│
├── templates/                  # HTML шаблоны
│   ├── index.html             # Главная страница
│   ├── login.html             # Страница входа
│   ├── register.html          # Страница регистрации
│   ├── profile.html           # Профиль пользователя
│   └── admin/                 # Админ-панель (создайте по необходимости)
│
└── static/                     # Статические файлы
    ├── css/
    │   ├── style.css          # Основные стили
    │   └── auth.css           # Стили авторизации
    └── js/
        └── script.js          # JavaScript
```

## 🚀 Запуск приложения

### Первый запуск (инициализация БД):

```bash
python app.py
```

При первом запуске:
- Автоматически создается база данных `prestige_motors.db`
- Создается админ-аккаунт:
  - **Email**: admin@prestigemotors.com
  - **Password**: admin123
- Добавляются 6 образцов автомобилей

### Последующие запуски:

```bash
python app.py
```

Приложение будет доступно по адресу: **http://localhost:5000**

## 👤 Учетные записи по умолчанию

### Администратор:
- **Email**: admin@prestigemotors.com
- **Password**: admin123

⚠️ **ВАЖНО**: Измените пароль администратора после первого входа!

## 📱 Основные маршруты

| Маршрут | Описание |
|---------|----------|
| `/` | Главная страница |
| `/register` | Регистрация |
| `/login` | Вход |
| `/logout` | Выход |
| `/profile` | Профиль пользователя |
| `/admin` | Панель администратора |
| `/admin/cars` | Управление автомобилями |
| `/admin/inquiries` | Просмотр запросов |

## 🔒 Безопасность

- Пароли хешируются с помощью Bcrypt
- CSRF защита через Flask
- SQL Injection защита через SQLAlchemy ORM
- Сессии с защитой

⚠️ **Для продакшена**:
1. Измените `SECRET_KEY` в `app.py`
2. Используйте PostgreSQL вместо SQLite
3. Включите HTTPS
4. Настройте переменные окружения для чувствительных данных

## 🎨 Особенности дизайна

- Адаптивный дизайн для всех устройств
- Плавная одностраничная навигация
- Темная тема с золотыми акцентами
- Анимации и hover-эффекты
- Система уведомлений (flash messages)

## 📊 База данных

### Таблицы:
1. **User** - пользователи
2. **Car** - автомобили
3. **Inquiry** - запросы клиентов
4. **Favorite** - избранные автомобили пользователей

## 🔧 Настройка

### Изменение SECRET_KEY (обязательно для продакшена):

В файле `app.py` найдите:
```python
app.config['SECRET_KEY'] = 'your-secret-key-change-this-in-production'
```

И измените на:
```python
app.config['SECRET_KEY'] = 'ваш-случайный-секретный-ключ-длиной-минимум-32-символа'
```

### Использование PostgreSQL вместо SQLite:

В файле `app.py` измените:
```python
app.config['SQLALCHEMY_DATABASE_URI'] = 'postgresql://username:password@localhost/prestige_motors'
```

## 📝 API Endpoints

### Публичные:
- `GET /` - Главная страница
- `GET /register` - Страница регистрации
- `POST /register` - Регистрация пользователя
- `GET /login` - Страница входа
- `POST /login` - Вход пользователя
- `POST /contact` - Отправка запроса

### Требующие авторизации:
- `GET /profile` - Профиль пользователя
- `POST /api/favorite/toggle/<car_id>` - Добавить/удалить из избранного
- `GET /logout` - Выход

### Только для администраторов:
- `GET /admin` - Панель администратора
- `GET /admin/cars` - Управление автомобилями
- `POST /admin/car/add` - Добавить автомобиль
- `POST /admin/car/edit/<id>` - Редактировать автомобиль
- `POST /admin/car/delete/<id>` - Удалить автомобиль
- `GET /admin/inquiries` - Просмотр запросов

## 🐛 Отладка

Для включения режима отладки убедитесь что в `app.py` установлено:
```python
app.run(debug=True)
```

⚠️ **НЕ используйте debug=True в продакшене!**

## 📞 Поддержка

При возникновении проблем:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что порт 5000 свободен
3. Проверьте логи в консоли
4. Убедитесь, что база данных создана корректно

## 🎯 Дальнейшее развитие

Возможные улучшения:
- [ ] Загрузка изображений автомобилей
- [ ] Email уведомления
- [ ] Фильтрация и поиск автомобилей
- [ ] Онлайн-бронирование тест-драйвов
- [ ] Интеграция с платежными системами
- [ ] Многоязычная поддержка
- [ ] API для мобильного приложения

## 📄 Лицензия

MIT License - свободное использование

---

**© 2026 Prestige Motors. All Rights Reserved.**
