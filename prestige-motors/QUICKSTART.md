# БЫСТРЫЙ СТАРТ 🚀

## Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 2: Инициализация базы данных

```bash
python init_db.py
```

Ответьте `yes` если хотите создать новую базу данных.

## Шаг 3: Запуск приложения

```bash
python app.py
```

## Шаг 4: Откройте в браузере

```
http://localhost:5000
```

## 👤 Учетные данные по умолчанию

### Администратор:
- Email: `admin@prestigemotors.com`
- Password: `admin123`

### Тестовые пользователи:
- Email: `john@example.com` | Password: `password123`
- Email: `jane@example.com` | Password: `password123`

---

## 📁 Структура файлов

Убедитесь, что у вас есть следующая структура:

```
prestige-motors/
├── app.py                    ← Главный файл приложения
├── init_db.py                ← Скрипт инициализации БД
├── requirements.txt          ← Зависимости
├── templates/
│   ├── index.html           ← Главная страница
│   ├── login.html           ← Страница входа
│   ├── register.html        ← Страница регистрации
│   └── profile.html         ← Профиль пользователя
└── static/
    ├── css/
    │   ├── style.css        ← Основные стили
    │   └── auth.css         ← Стили авторизации
    └── js/
        └── script.js        ← JavaScript
```

## ⚠️ Важно!

1. Измените `SECRET_KEY` в `app.py` перед деплоем!
2. Смените пароль администратора после первого входа!
3. Для продакшена используйте PostgreSQL вместо SQLite

## 🎉 Готово!

Теперь вы можете:
- ✅ Регистрировать пользователей
- ✅ Просматривать автомобили
- ✅ Добавлять в избранное
- ✅ Отправлять запросы
- ✅ Управлять через админ-панель (для администраторов)
